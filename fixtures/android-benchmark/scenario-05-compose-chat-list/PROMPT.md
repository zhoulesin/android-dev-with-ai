# Prompt（考卷 E）

```
用 Jetpack Compose 实现聊天列表页 ChatScreen。

技术栈：Kotlin 2.x，Compose BOM 2025.02，Coil 3.x。
数据类：ChatMessage(id, text, avatarUrl, timestampMillis, isMine)。

要求见 scenario-05 README 验收清单。只输出 Kotlin 代码，附必要 import。
```
