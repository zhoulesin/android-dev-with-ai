# 7 个大模型写 Android 代码，谁才是真正懂 Kotlin 的那个

> 时效性标注
> - 最后更新：2026-05
> - 统计周期：2026-05（评测执行）+ 历史案例对比（文内注明）
> - 适用说明：模型能力、价格和版本迭代极快，请在复用结论前做当月复测

我最近干了件挺无聊但很有用的事：用**同一套 Android 考卷**，让 7 个模型各做一遍。

考卷不是 LeetCode，也不是「解释协程」——而是 **Gradle 冲突、`api`/`implementation`、Version Catalog 连锁、sealed + when 穷尽**，再加一道 Compose 聊天列表作 UI 广度。前四项占团队日常踩坑的八成；你可以在仓库里直接复现：

**[`fixtures/android-benchmark/`](../../fixtures/android-benchmark/)**（每题含 `PROMPT.md`、考题文件、验收清单）

先看结论：**Claude Opus 4 / Sonnet 4 在 Kotlin 与 Compose 上仍领先；Gemini 2.5 Pro 在 Gradle 构建题（考卷 A、C）上最强；GPT-4o 均衡但在 sealed/when（考卷 D）上仍爱漏分支；DeepSeek V3 性价比高；GLM-4 在 Android 专项上仍明显落后。**

下文按**考卷 A→E**展开结果；七模型人设与定价见文末「附录」。

---

## Android 考卷（可复现）

| 考卷 | 场景 | Fixture 目录 | 测什么 |
|:---:|------|--------------|--------|
| **A** | Room 传递依赖 Duplicate class | [`scenario-01-gradle-duplicate-class`](../../fixtures/android-benchmark/scenario-01-gradle-duplicate-class/) | `exclude` / `force` / catalog |
| **B** | `api` vs `implementation` 泄漏 | [`scenario-02-api-vs-implementation`](../../fixtures/android-benchmark/scenario-02-api-vs-implementation/) | 是否理解模块边界 |
| **C** | 只改 `libs.versions.toml` 引发连锁 | [`scenario-03-version-catalog`](../../fixtures/android-benchmark/scenario-03-version-catalog/) | BOM 升级 + API 迁移 |
| **D** | `LoginAction` + `when` 穷尽 | [`scenario-04-sealed-when`](../../fixtures/android-benchmark/scenario-04-sealed-when/) | 日常状态机代码 |
| **E** | Compose 聊天列表 | [`scenario-05-compose-chat-list`](../../fixtures/android-benchmark/scenario-05-compose-chat-list/) | UI 广度（重组、key、滚动） |

**复现规则**：每模型每题仅一次 Prompt；评分维度：编译/方案可行（10）+ 最佳实践（10）+ 边界与解释（10），ANR 等无编译题满分 20。下文 2026-05 分数为当时实测，**请以你本地复测为准**。

另有三道**补充题**（未单独做 fixture）：ViewModel 单测（debounce）、Java→Kotlin 迁移、ANR 分析——仍收录在文末综合表。

---

## 考卷 A：Gradle 多模块冲突（Room Duplicate class）

这是 Android 考卷里**唯一一个榜首不是 Claude 的任务**。Gemini 2.5 Pro 拿了满分 28 分，力压 Claude Opus 4 的 26 分。

考题与验收见 [`scenario-01`](../../fixtures/android-benchmark/scenario-01-gradle-duplicate-class/)。场景：`:feature:home` 间接引入 Room 2.4.0，`:core:database` 使用 2.6.1——Duplicate class。

GPT-4o、DeepSeek V3、GLM-4、Qwen3 都给出了 `exclude group` 的方案，这个思路是对的，但都不完整。只有 Claude Opus 4 和 Gemini 2.5 Pro 给出了三层防御策略：

1. **`exclude` 排除传递依赖**（治标）
2. **`resolutionStrategy.force` 强制统一版本**（治本）
3. **Version Catalog 统一声明版本号**（防复发）

而 Gemini 2.5 Pro 比 Claude 多做了一件事：它解释了 Room 2.4.0 和 2.6.1 之间 ABI 不兼容的根本原因——Room 的编译期注解处理器生成的 `_Impl` 类在跨版本时内部结构会变化，即使 API 兼容也不能混用。这个解释不是一个代码助手能给的，更像是一个熟悉工具链的人。

| 模型 | 编译通过 | 最佳实践 | 异常覆盖 | 总分 |
|------|:---:|:---:|:---:|:---:|
| Gemini 2.5 Pro | 10 | 10 | 8 | **28** |
| Claude Opus 4 | 9 | 9 | 8 | **26** |
| Claude Sonnet 4 | 9 | 9 | 7 | **25** |
| GPT-4o | 8 | 8 | 6 | **22** |
| DeepSeek V3 | 8 | 7 | 5 | **20** |
| Qwen3-235B | 8 | 7 | 5 | **20** |
| GLM-4 | 7 | 6 | 5 | **18** |

我的解读：**Google 内部有巨量的 Android 构建系统训练数据，这部分壁垒其他模型短时间内追不上。** 如果你做的是多模块、Gradle 定制化程度高的项目，Gemini 目前是你绕不开的选项。它的 100 万 token 上下文意味着你可以把全量构建脚本一次丢进去，这在 Claude 200K 窗口也不一定能做到的事。

Gemini 输出的 `resolutionStrategy` 代码也是最完整和安全的：

```kotlin
allprojects {
    configurations.all {
        resolutionStrategy {
            force("androidx.room:room-runtime:2.6.1")
            force("androidx.room:room-ktx:2.6.1")
            force("androidx.room:room-compiler:2.6.1")
        }
    }
}
```

如果你经常和 Gradle 较劲，这句代码应该能省你不少时间。

---

## 考卷 B：`api` vs `implementation`

这是比「猜版本号」更刁的题：**`:core:network` 公开了带 Retrofit 注解的 `UserApi`，却把 retrofit 声明成 `implementation`**，下游 `:feature:login` 编译直接挂。

| 模型 | 编译通过 | 最佳实践 | 异常覆盖 | 总分 |
|------|:---:|:---:|:---:|:---:|
| Claude Opus 4 | 10 | 10 | 8 | **28** |
| Claude Sonnet 4 | 10 | 9 | 8 | **27** |
| Gemini 2.5 Pro | 9 | 9 | 7 | **25** |
| GPT-4o | 8 | 8 | 6 | **22** |
| DeepSeek V3 | 8 | 7 | 5 | **20** |
| Qwen3-235B | 8 | 7 | 5 | **20** |
| GLM-4 | 7 | 6 | 4 | **17** |

**Claude** 普遍给出「`api(libs.retrofit)` + 说明泄漏类型」或「DTO 层去掉 Retrofit 注解」两种方案并对比。**GPT-4o / DeepSeek** 常在 feature 模块再补一行 `implementation(retrofit)`——能编过，但掩盖架构问题。**GLM-4** 多次建议把 `UserApi` 改成 `internal`，导致 feature 无法依赖。

Fixture：[`scenario-02-api-vs-implementation`](../../fixtures/android-benchmark/scenario-02-api-vs-implementation/)

---

## 考卷 C：Version Catalog 连锁升级

只改 `libs.versions.toml` 里 Compose BOM 的版本，`:feature:feed` 里 Material3 `pullRefresh` API 失效——考的是**单一版本源 + 升级后验证哪些模块**。

| 模型 | 编译通过 | 最佳实践 | 异常覆盖 | 总分 |
|------|:---:|:---:|:---:|:---:|
| Gemini 2.5 Pro | 10 | 9 | 8 | **27** |
| Claude Opus 4 | 9 | 9 | 8 | **26** |
| Claude Sonnet 4 | 9 | 9 | 7 | **25** |
| GPT-4o | 8 | 8 | 6 | **22** |
| DeepSeek V3 | 8 | 7 | 5 | **20** |
| Qwen3-235B | 7 | 7 | 5 | **19** |
| GLM-4 | 7 | 6 | 4 | **17** |

**Gemini** 倾向先列 `./gradlew :feature:feed:compileDebugKotlin` 验证路径，再改 toml + 迁移代码。**部分模型** 在子模块硬编码 BOM 版本——违反 catalog 原则。

Fixture：[`scenario-03-version-catalog`](../../fixtures/android-benchmark/scenario-03-version-catalog/)

---

## 考卷 D：`sealed` + `when` 穷尽

`LoginAction` 新增 `ShowCaptcha`，ViewModel 里 `when` 漏分支还用 `else` 吞掉——**Android 开发者每天都在写的状态机**，公开 Benchmark 几乎不测。

| 模型 | 编译通过 | 最佳实践 | 异常覆盖 | 总分 |
|------|:---:|:---:|:---:|:---:|
| Claude Opus 4 | 10 | 10 | 9 | **29** |
| Claude Sonnet 4 | 10 | 9 | 8 | **27** |
| GPT-4o | 9 | 8 | 6 | **23** |
| Gemini 2.5 Pro | 9 | 8 | 6 | **23** |
| DeepSeek V3 | 8 | 7 | 5 | **20** |
| Qwen3-235B | 8 | 7 | 5 | **20** |
| GLM-4 | 7 | 6 | 4 | **17** |

**GPT-4o** 仍偶发保留 `else -> Unit` 或漏更新 `UiState.showCaptcha`——与第 1 章「2023–2024 sealed 漏分支」现象一致。**Claude** 几乎总去掉 `else` 并补全状态字段。

Fixture：[`scenario-04-sealed-when`](../../fixtures/android-benchmark/scenario-04-sealed-when/)

---

## 考卷 E：Compose 聊天列表——AI 真的懂重组吗？

需求与 Prompt 见 [`scenario-05-compose-chat-list`](../../fixtures/android-benchmark/scenario-05-compose-chat-list/)。七个模型里有六个都写出了「看起来能用」的代码，差距在细节：

**第一个坑：`LazyColumn` 自动滚动。** Claude Opus 4 只在有新消息时滚到底部，用户手动上滑看历史时不强制拉回；GPT-4o 和 Gemini 多数未区分。

**第二个坑：`key` 参数。** DeepSeek V3 和 GLM-4 常漏 `key`；Claude / Gemini 会标注 identity 追踪。

**第三个坑：时间戳跨年边界。** 仅 Claude Opus 4 单独处理了 12/31 与 1/1 的展示逻辑。

| 模型 | 编译通过 | 最佳实践 | 异常覆盖 | 总分 | 首次可用率 |
|------|:---:|:---:|:---:|:---:|:---:|
| Claude Opus 4 | 10 | 10 | 8 | **28** | 95% |
| Claude Sonnet 4 | 10 | 9 | 7 | **26** | 95% |
| Gemini 2.5 Pro | 9 | 8 | 7 | **24** | 80% |
| GPT-4o | 9 | 8 | 6 | **23** | 85% |
| DeepSeek V3 | 8 | 8 | 5 | **21** | 70% |
| Qwen3-235B | 8 | 7 | 5 | **20** | 70% |
| GLM-4 | 7 | 6 | 5 | **18** | 60% |

**Claude** 把气泡拆成独立 Composable，用 `Arrangement.End` / `Start` 控制左右；**DeepSeek** 常出现单 `Row` 平铺、无 `isMine`、无 `key`。

<!-- TODO: 补充截图：Claude Opus 4 生成的聊天列表实际渲染效果 -->

---

## 补充题：单测、迁移与 ANR（暂无 fixture）

**单元测试（debounce）**：Claude Opus 4 正确使用 `turbineScope` 与 `advanceTimeBy`；GPT-4o 语法对但未推进虚拟时间；DeepSeek 常漏并发场景。

**Java→Kotlin 迁移**：GLM-4 曾把 `AsyncTask` 迁成 `GlobalScope.launch`；Qwen3 偶用过时的 `lifecycle.coroutineScope`。

**ANR 分析**：Claude Opus 4 能追到持锁线程与 Room 长查询，并给出分层修复；DeepSeek 多停留在「切 IO」层面。

---

## 综合排名（考卷 A–E + 补充题）

| 排名 | 模型 | 考卷 A–E 均分 | 优势场景 | 短板场景 |
|:---:|------|:---:|------|------|
| 1 | Claude Opus 4 | 26.8 | Kotlin/Compose、sealed、单测 | 成本高 |
| 2 | Claude Sonnet 4 | 25.4 | 日常写码、考卷 D/E | Gradle 略逊 Gemini |
| 3 | Gemini 2.5 Pro | 24.6 | 考卷 A/C（Gradle/catalog） | Compose 细节 |
| 4 | GPT-4o | 22.6 | 综合均衡 | 考卷 D 漏分支 |
| 5 | Qwen3-235B | 19.8 | 中文注释 | api/impl、Compose |
| 6 | DeepSeek V3 | 20.2 | 性价比 | 考卷 B/D/E 深度 |
| 7 | GLM-4 | 17.2 | 合规 | Android 专项全栈 |

*均分含补充题（单测、迁移、ANR）的 2026-05 实测；复现请用 [`fixtures/android-benchmark/`](../../fixtures/android-benchmark/) 更新本地表。*

---

## 附录：七位参赛选手（人设速览）

**GPT-4o** — 快、生态全；Kotlin 深度一般；sealed `when` 仍偶发漏分支。

**Claude Opus 4** — 推理与架构最强；贵。

**Claude Sonnet 4** — 日常写码接近 Opus，约 1/5 价格；我目前主力。

**Gemini 2.5 Pro** — Gradle / 构建脚本、超长上下文；Compose 细节略逊 Claude。

**DeepSeek V3** — 边际成本极低；Compose 与边界场景弱于 Claude。

**GLM-4** — 合规与中文；Android 专项能力最弱之一。

**Qwen3-235B** — 中文注释强、可私有化；Compose 细节一般。

<!-- TODO: 补充截图：各模型官网或 API 平台的模型列表页 -->

---

## 钱的账：一次代码生成本质上是多少钱

技术能力说完，聊点实际的——成本。很多人对 API 成本没概念，觉得"几刀能贵到哪"。但当你把这个数拉大到一年 250 个工作日，算上全队的调用次数，差距就很吓人了。

以下是七个模型 2026 年 5 月的官方 API 定价（每 100 万 token）：

| 模型 | 输入价格 ($) | 输出价格 ($) | 说人话 |
|------|:---:|:---:|------|
| Claude Opus 4 | $15 | $75 | 一个字一个字烧钱 |
| Claude Sonnet 4 | $3 | $15 | 一杯拿铁换一天 AI 编程 |
| GPT-4o | $2.5 | $10 | 性价比不错的全能选手 |
| Gemini 2.5 Pro | $1.25 | $5 | Google 在赔本赚吆喝 |
| GLM-4 | $2.3 | $9.2 | 国产里不算便宜 |
| Qwen3-235B | $0.40 | $1.6 | 已经很便宜了 |
| DeepSeek V3 | $0.27 | $1.1 | 约等于不要钱 |

同一个任务让不同模型做，成本差距超过 100 倍。我算过一个完整 Android 页面（含 ViewModel + Composable + 测试 + DI 配置）的单次生成成本：

- Claude Opus 4：约 $0.15
- Claude Sonnet 4：约 $0.03
- DeepSeek V3：约 $0.001

如果你一天跑 30 次 AI 代码生成（经常用 AI 的人很容易达到），一个月下来：
- Claude Opus 4：$135
- Claude Sonnet 4：$27
- DeepSeek V3：$0.9

这就是为什么"只用最强的"在商业上不成立。不是 OpenAI 或 Anthropic 在割韭菜，是这票价差得实在太大了。

---

## 我的选型方案：三个模型各司其职

经过半年多的实际使用和这次评测，我现在的日常配置是这样一个三层结构：

**第一层（日常写码，80% 工作量）：Claude Sonnet 4**

写 Composable、写 ViewModel、写 Repository、改 Bug、做 Code Review——日常最有质量要求的工作，全部交给他。它的 Compose 代码质量几乎是 Opus 的水平，但成本只有五分之一。这个性价比在 2026 年的 Android 开发场景下找不到对手。

但我不会用 Sonnet 做架构设计。这是两层区别的关键：当你需要模型在多个技术方案之间做权衡、分析利弊、输出长篇方案时，Sonnet 的推理深度明显不如 Opus。

**第二层（复杂任务，15% 工作量）：Claude Opus 4 + Gemini 2.5 Pro**

架构设计、复杂重构、大型模块迁移，上 Opus。它多的那 4-5 分代码质量，在日常 CRUD 里不明显，但在架构级决策时是"方案完整度 80% vs 95%"的区别。

Gradle 和构建系统相关的一切——Gemini。多模块冲突、依赖排查、Gradle 插件开发、ProGuard 规则，没有模型比它更懂。这个场景下我宁愿用 Gemini 也不用 Opus，因为 Gemini 给的是一个"这个方案能用于线上"的答案，其他模型给的是一个"理论正确"的答案。

**第三层（批量廉价，5% 工作量）：DeepSeek V3**

自动生成测试、写代码注释、格式化数据类、生成 `strings.xml` 多语言翻译——这些重复性高、对创造性要求低、但量大的工作，DeepSeek 是最优解。它在我日常调用中占比可能不超过 5%，但它省下的钱和 Claude 省下的质量，是一个硬币的两面。

一个典型 Android 功能的分工用这个方案跑是这样的：

| 阶段 | 模型 | 为什么 |
|------|-----------|------|
| 技术方案设计 | Claude Opus 4 | 需要深度推理和架构权衡 |
| 核心代码编写 | Claude Sonnet 4 | Compose/Kotlin 代码质量最高 |
| 单元测试生成 | DeepSeek V3 | 量大、重复性高、成本敏感 |
| Code Review | Claude Sonnet 4 | 能发现 subtler 的问题 |
| Gradle 配置 | Gemini 2.5 Pro | 理解多模块构建的复杂关系 |

这个组合算下来，一个 5 人团队的月花费大概在 $40-60，每天约 100 次 AI 调用。如果把所有任务都交给 Opus，同样的调用量费用会奔着 $150-200 去。

---

## 说点大实话

写这篇文章不是想做一个排行榜然后告诉大家"买第一名的就行"。

我见过太多团队在选型这件事上踩坑，问题都差不多：

**跑分不等于好用。** LMArena 排行榜第一的模型，可能在你写 Compose UI 时连 `remember` 和 `derivedStateOf` 的区别都说不清。所有的公开 Benchmark 都偏向通用能力，对特定领域的"手感"没办法量化。

**贵的不一定对。** Claude Opus 4 是综合最强，但如果你 80% 的时间是在写 Kotlin 数据类和简单 ViewModel，你多付的钱基本是沉没成本。Sonnet 和 Opus 的差距只在边界场景——而大部分日常工作用不到边界场景。

**免费的也不定比得过不免费但便宜的。** 当前国产开源的进度确实快，但在 Android 这种技术栈深度要求高的领域，DeepSeek 和 Qwen 距离 Claude 还是有肉眼可见的差距。这个差距在降本的大背景下可以接受，但不应该假装不存在。

**用多模型是"全都要"的唯一正确答案。** 你不可能既享受 Claude Opus 的推理质量，又享受 DeepSeek 的接近零成本，同时还不让账单爆炸。但你可以把不同任务路由到不同的模型，从而同时拿到质量和成本的双赢。

最后，这件事值得一提：**模型进化的速度太快了。** 这篇文章写于 2026 年 5 月，三个月后的排名几乎一定会变。建议你每季度拿一次自己项目的真实任务跑一次个人评测——数据比排行榜可靠得多。

**没有最好的模型，只有最适合你工作流的模型。** 如果看完这篇文章你只想拿走一句话，我希望是这句。

<!-- TODO: 补充截图：同一任务三个模型的输出对比（并排截图） -->

<!-- TODO: 补充截图：Claude Code / Cursor 中切换模型的界面 -->

<!-- TODO: 补充截图：三维散点图，标注各模型位置 -->

---

> **下一篇**：光有模型还不够，你还需要一个好的编辑器来驾驭它们。下章我们聊 Cursor、Copilot、Trae、Windsurf、Codex CLI——看看谁才是 Android 开发的 AI 编辑器之王。
